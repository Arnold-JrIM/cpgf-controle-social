name: joint-retrieval-flow-attribution

on:
  pull_request:
    paths:
      - "data/manifests/joint_retrieval_flow_attribution_1_0_0.json"
      - "tests/unit/test_joint_retrieval_flow_attribution.py"
      - "tests/unit/test_versions.py"
      - ".github/workflows/joint_retrieval_flow_attribution.yml"
  workflow_dispatch:

jobs:
  historical-evidence:
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - run: python -m pip install -e ".[dev]"
      - name: Validate frozen post-hoc evidence
        run: |
          pytest -q tests/unit/test_joint_retrieval_flow_attribution.py
      - name: Validate historical manifest contract
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          manifest = json.loads(
              Path("data/manifests/joint_retrieval_flow_attribution_1_0_0.json").read_text(
                  encoding="utf-8"
              )
          )
          assert manifest["status"] == "POST_HOC_DIAGNOSTIC_FROZEN"
          assert manifest["frozen_flow"]["router_version"] == "1.2.0"
          assert manifest["frozen_flow"]["retrieval_planner_version"] == "1.1.0"
          result = manifest["results"]
          assert result["actual_joint_exact"] == 12
          assert result["router_only_failures"] == 15
          assert result["planner_only_failures"] == 1
          assert result["shared_router_planner_failures"] == 12
          assert manifest["governance"]["tuning_performed_in_increment"] is False
          PY
