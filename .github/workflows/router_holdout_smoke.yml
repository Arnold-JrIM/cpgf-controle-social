name: router-holdout-smoke

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  holdout:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install project
        run: python -m pip install -e ".[dev]"

      - name: Evaluate Router 1.0.0 on untouched holdout
        run: |
          python scripts/evaluate_router_holdout.py --output router_holdout_v1_0_0.json
          python - <<'PY'
          import json
          from pathlib import Path

          payload = json.loads(Path("router_holdout_v1_0_0.json").read_text(encoding="utf-8"))
          summary = payload["routing"]["summary"]
          assert payload["holdout_version"] == "1.0.0"
          assert payload["router_version"] == "1.0.0"
          assert payload["question_overlap_exact"] == 0
          assert summary["cases"] == 40
          assert summary["supported_target_cases"] == 40
          print("ROUTER HOLDOUT 1.0.0: MEASURED")
          print("holdout_sha256:", payload["holdout_sha256"])
          print(json.dumps(summary, ensure_ascii=False, indent=2))
          print(json.dumps(payload["diagnostics"], ensure_ascii=False, indent=2))
          PY

      - name: Upload holdout evaluation
        uses: actions/upload-artifact@v4
        with:
          name: router-holdout-v1.0.0-evaluation
          path: router_holdout_v1_0_0.json
          if-no-files-found: error
          retention-days: 14
